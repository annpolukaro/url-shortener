env: "local"

storage:
  type: "postgres"
  path: "postgres://postgres:postgres@postgres:5432/url_shortener?sslmode=disable"

http_server:
  address: "0.0.0.0:8082"
  timeout: 4s
  idle_timeout: 60s
  user: "myuser"
  password: "mypass"