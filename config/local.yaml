env: "local"

storage:
  type: "postgres"
  path: "postgres://postgres:postgres@127.0.0.1:5433/url_shortener?sslmode=disable"

http_server:
  address: "localhost:8082"
  timeout: 4s
  idle_timeout: 60s
  user: "myuser"
  password: "mypass"